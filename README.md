# Claude Chat v16（开源版）使用说明

## 这是什么

一个 AI 对话前端，单个 HTML 文件，双击打开即可在本地使用，也可以部署到服务器供多人访问。支持 Claude（Anthropic）和任何 OpenAI 兼容接口。内置纪念日时间轴、共享相册、AI 语音（ElevenLabs TTS）等情感向功能，可选接入蓝牙玩具控制和自建的「心跳唤醒」后端。

请求直接从你的浏览器发到 AI 服务商，**不经过任何第三方服务器**（心跳同步除外，见下方说明）。

> 本版本为开源发布版：已移除原作者本机的桥接地址、服务器地址等个人配置，所有连接地址均为通用默认值，每个使用者需要按自己的环境自行填写。

---

## 快速开始

1. 双击打开 `claude-chat-v16-opensource.html`
2. 点击右上角 **设置（⚙）**
3. 在 **API** 标签页填入你的 API Key
4. 选好模型，保存，开始对话

> 本地直接使用无需任何服务器，所有数据保存在本机浏览器中。心跳同步是完全可选的加分项，不填就跟普通聊天前端一样。

---

## 设置说明

### API 标签页

| 项目 | 说明 |
|------|------|
| API Key | 你的 Anthropic 或 OpenAI 兼容 API Key |
| Base URL | 默认 `https://api.anthropic.com`，使用中转或其他服务时修改 |
| API 类型 | Anthropic（Claude）或 OpenAI 兼容 |
| 模型 | 填写模型名称，或点“获取列表”自动拉取 |
| **心跳同步地址** | **可选。**留空则不启用。填写你自建心跳服务器的地址（例如 `http://你的服务器IP:5050`），会自动把当前对话同步过去，并定期拉取它主动生成的消息，详见下方「心跳唤醒（可选）」 |
| 最大 Token | 单次回复最大长度 |
| Temperature / Top P | 控制回复随机性，默认值适合大多数场景 |
| 上下文条数 | 每次发送携带的历史消息数，0 = 不限 |
| 系统提示词 | 设定 AI 角色或行为规则 |

### MCP 标签页

填入 MCP 服务地址可为 AI 接入外部工具（如搜索、文件操作等）。每行一个 URL，保存后自动连接。

---

## 心跳唤醒（可选）

配合另外一套自建的 Python 后端（`server.py` + `heartbeat.py`，跑在你自己的服务器/Termux 上），实现「AI 在你长时间没说话时，自己判断要不要主动联系你，要联系就通过 Bark 推送到手机」。

### 工作原理

```
本前端（这个HTML）
    │ 每次保存对话 → 自动同步给心跳服务器（3秒防抖）
    │ 每次你发消息 → 通知心跳服务器"刚互动过"
    │ 每30秒 → 拉取心跳服务器有没有新的主动消息
    ▼
你的心跳后端（server.py 存数据 + heartbeat.py 判断要不要说话）
    │ 空闲够久 → 用你在这里填的同一份 API 配置，自己调用 AI
    │ AI 判断要不要主动开口，要说就发 Bark 推送到你手机
    ▼
  同时把这句话写回来，下次前端拉取时会自动合并进聊天记录
```

**心跳只是一个独立的"旁路观察者"**，不是网关，不会站在你和 AI 的正常对话中间转发——你平时怎么聊天完全不受影响，它只是隔一段时间偷看一眼聊天记录，自己决定要不要插一句话。

### 使用方法

1. 在你自己的服务器/Termux 上部署心跳后端（Python 脚本另外发布，不包含在这个 HTML 文件里）
2. 打开本前端 → 设置 → API → 「心跳同步地址」填心跳服务器的地址（例如 `http://123.45.67.89:5050`）
3. 保存后正常聊天即可，不需要做任何其他操作

### 重要提示

- 只会同步**当前正在看的这一个对话窗口**，如果你有多个窗口，心跳只关心你当前打开的那个
- 心跳生成的消息会带上 `heartbeat: true` 标记，从视觉上跟你正常聊天的内容没有区别，都会出现在聊天记录里
- 如果不填这项，本功能完全不生效，跟普通版没有任何区别

---

## 主要功能

### 多窗口对话
左侧边栏可新建、切换、重命名对话窗口，各窗口历史独立保存。

### 持久记忆
AI 可以在对话中主动更新"记忆"，下次对话时自动带入。记忆内容可在设置中查看和手动编辑。顶栏显示"🧠 记忆已启用"时表示有记忆内容生效。

### 图片输入
可粘贴图片（Ctrl+V）或点击附件按钮上传，发送给支持视觉的模型。

### 发送与换行
- **Enter**：发送消息
- **Shift + Enter**：换行

### 导出聊天记录
点击顶栏右侧的 **↑ 导出** 按钮，可将当前对话窗口的聊天记录导出为：
- **Markdown (.md)**：格式整洁，适合阅读和分享
- **纯文本 (.txt)**：任何地方都能打开
- **JSON (.json)**：包含完整结构数据，适合程序处理
- **复制到剪贴板**：直接粘贴到备忘录等应用

**选择消息导出**
导出面板最下方新增"选择消息导出"入口。点击后进入选择模式：
- 每条聊天记录左上角出现勾选框，点击气泡任意位置或勾选框均可勾选/取消
- 底部工具栏可"全选/取消全选"，显示已选条数
- 点击"导出"后可选择 Markdown / 纯文本 / JSON / 复制到剪贴板，四种格式都只包含勾选中的那几条消息，并按原始时间顺序排列
- 导出完成后自动退出选择模式

导出内容仅包含文字消息，图片不随历史持久化，也不参与选择导出。

### 调试工具
点击顶栏右侧的 **🔍 调试** 按钮可开启调试模式。开启后，下次发送含图片的消息时会弹窗显示实际发送给 API 的内容（图片数量、base64 片段、文字内容），方便排查图片识别异常或请求格式问题。再次点击关闭。

### MCP 工具调用结果
AI 调用 MCP 工具时，对话中会出现一个可折叠的工具块，点击展开后可以看到：
- **调用参数**：AI 传给工具的具体入参（JSON 格式）
- **返回结果**：工具实际返回给 AI 的原始内容

方便确认工具是否正常执行、返回了什么数据。

### 环境感知
启用定位权限后，AI 会自动获知当前时间、天气、位置，顶栏会显示当前天气状态。

---

## 玩具控制（可选）

需要配合 `toy_bridge.py` 本地桥接脚本使用，适用于 SVAKOM Alberta。

### 启动桥接

```bash
pip install bleak websockets
python toy_bridge.py
```

运行前先用文本编辑器打开 `toy_bridge.py`，将 `DEVICE_ADDRESS` 改为你的玩具蓝牙地址（用 `scan_toy.py` 扫描获取）。

脚本启动后默认监听 `ws://127.0.0.1:8765`。

### 连接玩具

设置面板 → 玩具标签页 → 桥接地址默认为 `ws://127.0.0.1:8765`（本机运行桥接脚本时无需修改；如果桥接脚本运行在局域网内的其他设备上，改成该设备的局域网 IP，例如 `ws://192.168.x.x:8765`）→ 点击连接。

### 手动控制

| 按钮 | 说明 |
|------|------|
| 阵雷 / 雨点 / 鼓点 / 水滴 | 吮吸模式 1-4，各有不同节奏 |
| 持续 | 吮吸模式 5，持续吮吸 |
| 弱 / 中 / 强 | 吮吸强度，切换后立刻生效 |
| 振动滑块 | 振动强度 0-10 |
| 停止 | 停止所有动作 |

### AI 控制模式

开启"AI 控制"后，AI 回复时会自动附带玩具指令。

- **剧本模式**：AI 回复结束后播放一段预设动作序列（5-15 步，总时长 ≤30 秒）
- **实时模式**：AI 回复过程中逐步插入控制指令，随内容实时响应
- **氛围自驱模式**：AI 根据对话情境自动周期性触发玩具动作，无需用户每次手动触发

---

## 纪念日时间轴（可选）

一个你们共同维护的纪念日列表，入口在输入框「＋」面板。

- **添加/编辑**：名称 + 日期 + 重复规则（每年 / 每月 / 每周 / 仅一次）+ 备注。日期边界自动处理（如 2/29 在平年自动跳过、31 号在小月收敛到月末）
- **时间轴视图**：按下次发生日期排序，显示倒计时（今天 / 明天 / N 天后）、第 N 年/月/周
- **当天悬浮横条**：有纪念日的那天，聊天界面顶部自动出现提醒横条，点击查看详情；平时完全隐藏
- **AI 也能维护**：AI 可以在回复里用隐藏标签帮你添加或删除纪念日，也会在纪念日当天收到全天提醒、主动送上祝福
- 所有数据保存在本机浏览器，纯本地计算

## 共享相册（可选，需自建后端）

你和 AI 共同的相册，支持多相册管理。AI 的访问是**分层的**，平时只看到相册清单（几乎不耗 token），想回忆时自己调工具逐层打开：

1. album_list — 看有哪些相册（名称+张数）
2. album_photos — 看某相册的照片描述清单（每张照片一句 15 字内描述）
3. photo_view — 按描述挑一张，查看真图
4. photo_save — 把你发的照片存进相册（自动附描述，相册不存在会自动创建）

你可以在前端直接上传、删除、改描述、建/删相册；AI 也可以通过工具做同样的事。

### 后端部署

相册需要一个极小的存储服务（Flask 单文件，零额外依赖），见 photo-album/ 目录：

    # 服务器上
    pip install flask
    cp photo-album/.env.example photo-album/.env   # 填入 ALBUM_TOKEN（随机 hex）等
    pm2 start python3 --name photo-album -- /path/to/photo-album/server.py

服务监听 127.0.0.1:8086，建议用 Nginx 反代并限制路径（只透传 /photo-api/），前端「设置 → 相册」里填服务地址和 Token。

## AI 语音（可选，需 ElevenLabs）

AI 可以自己决定什么时候「开口说话」——回复里附带一段语音，前端渲染成仿微信的语音条（显示秒数、点击播放、播放中有声波动画）。

- 需要 ElevenLabs（elevenlabs.io）账号的 API Key（只需 Text-to-Speech 权限）和 Voice ID
- 支持保存多组 Key + Voice 配置组随时切换
- 生成走 photo-album 后端代理（/tts 端点），同一段文字只生成一次，之后命中缓存秒回、不重复消耗配额
- 语义：开关开启期间 AI 发的语音会保存，刷新后仍可回放；生成失败的不显示；关闭开关后 AI 不再收到语音相关提示

> 语音和相册共用同一个后端服务。不用这两个功能的话，后端可以完全不部署。

---
## 数据存储

所有数据（对话历史、设置、记忆）均保存在**本地浏览器 localStorage**，不上传任何地方。图片 base64 不持久化（刷新后图片历史不显示，但不影响对话功能）。

心跳同步开启后，对话历史会额外同步一份到你自己配置的心跳服务器（同样是你自己的服务器，不是第三方）。

---

## 部署到服务器（可选）

> 本地使用无需此步骤，直接双击 HTML 文件打开即可。

如果你希望在任何设备上通过浏览器访问，或分享给他人使用，可以将 HTML 文件部署到静态服务器。

### 方式一：直接上传静态文件（最简单）

适合有虚拟主机、对象存储或静态托管服务的情况。

将 `claude-chat-v16-opensource.html` 上传到以下任一平台即可通过链接访问：

- **Cloudflare Pages**：拖拽上传，免费，自带 CDN
- **Vercel / Netlify**：拖拽或 Git 部署，免费
- **GitHub Pages**：推送到仓库后开启 Pages 功能
- **阿里云 OSS / 腾讯云 COS**：开启静态网站托管，上传文件即可

### 方式二：用 Nginx 部署到 VPS

适合有自己服务器的情况。

```bash
# 将 HTML 文件上传到服务器
scp claude-chat-v16-opensource.html user@your-server:/var/www/html/

# 安装 Nginx（如未安装）
sudo apt install nginx

# Nginx 默认会托管 /var/www/html/ 下的文件
# 访问 http://your-server-ip/claude-chat-v16-opensource.html 即可
```

如需绑定域名并启用 HTTPS，可用 Certbot 申请免费证书：

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com
```

### 方式三：PM2 + Node.js 静态服务（云服务器长期运行）

适合有云服务器、用 SFTP 传文件、希望服务一直在线的情况。

**第一步：安装依赖（只需一次）**

```bash
# 安装 Node.js（如未安装）
sudo apt install nodejs npm

# 全局安装 http-server 和 pm2
npm install -g http-server pm2
```

**第二步：上传文件**

用 Termius、FileZilla 等 SFTP 工具将 `claude-chat-v16-opensource.html` 拖入服务器目录，例如 `/var/www/chat/`。

**第三步：用 PM2 启动并保活**

```bash
# 进入文件所在目录
cd /var/www/chat

# 用 pm2 启动静态服务，监听 8080 端口
pm2 start http-server -- -p 8080

# 设置开机自启
pm2 save
pm2 startup
```

启动后访问 `http://你的服务器IP:8080/claude-chat-v16-opensource.html` 即可。

**常用 PM2 命令**

```bash
pm2 list          # 查看运行中的服务
pm2 restart all   # 重启所有服务
pm2 stop all      # 停止所有服务
pm2 logs          # 查看日志
```

**可选：用 Nginx 反代并绑定域名**

如果希望通过域名访问并启用 HTTPS，可以在 PM2 前面加一层 Nginx：

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
    }
}
```

然后用 Certbot 申请证书：

```bash
sudo certbot --nginx -d yourdomain.com
```

### 方式四：Python 临时服务器（局域网共享）

适合临时在局域网内共享给其他设备使用：

```bash
# 在 HTML 文件所在目录执行
python -m http.server 8080
```

然后同一 WiFi 下的设备访问 `http://你的电脑IP:8080/claude-chat-v16-opensource.html` 即可。

### 注意事项

- 部署后每个用户需要自己填入 API Key，不会共用
- 心跳同步地址如果填的是局域网 IP，只有同一网络下能访问到；如果是公网服务器地址则不受此限制
- 玩具控制功能需要桥接脚本在**用户本地**运行，服务器部署不影响此功能
- 建议部署后启用 HTTPS，避免浏览器因安全限制阻止 API 请求

---

## 开源说明

本仓库/文件不包含任何原作者的个人配置（API Key、服务器地址、局域网 IP、心跳同步地址等）——所有可配置项均为空值或通用默认值。Fork 或二次分发时，请同样注意：

- 不要把自己的 API Key、Base URL、心跳同步地址提交进代码或截图分享
- 玩具桥接地址、`DEVICE_ADDRESS` 等如果修改为你本机的局域网 IP，也不建议连同代码一起公开分发
- localStorage 中保存的对话历史、记忆内容仅存在使用者本机浏览器中，不会随文件分发泄露

## 其他注意事项

- API Key 保存在本地浏览器，**不要在公共电脑上使用**
- 心跳同步功能完全可选，不填地址就是普通聊天前端，不会有任何额外的网络请求
- 玩具控制功能需要桥接脚本在本地运行，关闭脚本后自动断开
- 使用 OpenAI 兼容接口时，部分功能（如工具调用格式）可能有差异
